model:
  hidden_dim: 1024
  num_layers: 12
  num_heads: 8

training:
  batch_size: 32
  epochs: 50
  lr: 0.0003
  distributed: true

federated:
  enabled: true
  aggregation: secure_mean
  clients: 5

deployment:
  device: auto
  export_onnx: true